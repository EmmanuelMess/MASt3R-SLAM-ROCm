[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "dust3r"
version = "0.0.1"
dynamic = ["dependencies"]

[tool.setuptools.packages.find]
where = ["."]
include = ["dust3r*"]
namespaces = false
